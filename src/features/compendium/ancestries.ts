import type { Catalog } from "../../domain/catalog";
import type { AncestryDefinition, FeatureDefinition } from "../../domain/types";

export type AncestryFeatureState = { compendiumAncestrySearch: string; ancestryModalOpen: boolean; editingCompendiumAncestryId?: string; deletingCompendiumAncestryId?: string };
export type AncestryFeatureDependencies = { state: AncestryFeatureState; catalog: Catalog; escapeHtml: (value: string) => string; renderEmptyInline: (message: string) => string; getPackDisplayName: (packId: string) => string; saveCustomDefinition: (definition: AncestryDefinition | FeatureDefinition) => Promise<void>; deleteCustomDefinition: (definitionId: string) => Promise<void>; refreshCatalog: () => Promise<void>; render: () => void };

export function renderCompendiumAncestriesManager(deps: AncestryFeatureDependencies): string {
  const search=deps.state.compendiumAncestrySearch.trim().toLocaleLowerCase("pt-BR"),entries=[...deps.catalog.ancestries].filter(a=>!search||`${a.name} ${a.summary}`.toLocaleLowerCase("pt-BR").includes(search)).sort((a,b)=>a.name.localeCompare(b.name,"pt-BR"));
  return `<main class="content compendium-content"><div class="screen-title"><div><h1>Ancestralidades</h1><p>Defina linhagens reutilizaveis e as duas features que cada uma concede.</p></div><button class="secondary-action screen-title-action" type="button" data-action="back-compendium-index">Voltar ao indice</button></div><section class="compendium-management-panel"><div class="compendium-management-toolbar"><div class="management-copy"><span>HERANCA</span><strong>${deps.catalog.ancestries.length} ${deps.catalog.ancestries.length===1?"ancestralidade":"ancestralidades"}</strong></div><label class="search-box"><span aria-hidden="true">⌕</span><input type="search" data-compendium-ancestry-search value="${deps.escapeHtml(deps.state.compendiumAncestrySearch)}" placeholder="Pesquisar ancestralidade" aria-label="Pesquisar ancestralidade" /></label><button class="primary-action" type="button" data-action="new-compendium-ancestry">Nova ancestralidade</button></div>${entries.length?`<div class="compendium-ancestry-results">${entries.map(a=>renderAncestryResult(a,deps)).join("")}</div>`:deps.renderEmptyInline(search?"Nenhuma ancestralidade corresponde a esta pesquisa.":"Nenhuma ancestralidade cadastrada. Crie a primeira para disponibilizar suas Top e Bottom Features na criacao de personagens.")}</section></main>`;
}
export function renderCompendiumAncestryFormModal(deps: AncestryFeatureDependencies): string {if(!deps.state.ancestryModalOpen)return "";const a=deps.state.editingCompendiumAncestryId?deps.catalog.ancestries.find(x=>x.id===deps.state.editingCompendiumAncestryId):undefined,top=a?feature(a,"top",deps.catalog):undefined,bottom=a?feature(a,"bottom",deps.catalog):undefined;return `<div class="modal-backdrop" data-modal-backdrop><form class="modal ancestry-form-modal" onsubmit="return false;" aria-labelledby="ancestry-form-title"><button class="modal-close" type="button" data-modal-close aria-label="Fechar">×</button><div class="modal-title"><span>Compendium</span><h2 id="ancestry-form-title">${a?"Editar ancestralidade":"Nova ancestralidade"}</h2><p>As duas features ficam ordenadas para suportar escolhas unicas e mistas.</p></div><label class="form-field"><span>Nome</span><input data-ancestry-name required value="${deps.escapeHtml(a?.name??"")}" /></label><label class="form-field"><span>Descricao</span><textarea data-ancestry-summary required>${deps.escapeHtml(a?.summary??"")}</textarea></label><label class="form-field"><span>Imagem (opcional)</span><input data-ancestry-image type="file" accept="image/*" />${a?.image?"<small>A imagem atual sera mantida se nenhum arquivo for escolhido.</small>":""}</label>${featureFields("Top",top,"top",deps)}${featureFields("Bottom",bottom,"bottom",deps)}<p class="form-error" data-ancestry-error hidden></p><div class="modal-actions"><button class="secondary-action" type="button" data-modal-close>Cancelar</button><button class="primary-action" type="button" data-action="save-compendium-ancestry">Salvar ancestralidade</button></div></form></div>`;}
export function renderDeleteCompendiumAncestryModal(deps: AncestryFeatureDependencies): string {const a=deps.state.deletingCompendiumAncestryId?deps.catalog.ancestries.find(x=>x.id===deps.state.deletingCompendiumAncestryId):undefined;return a?`<div class="modal-backdrop" data-modal-backdrop><section class="modal confirm-modal"><h2>Excluir ancestralidade?</h2><p>"${deps.escapeHtml(a.name)}" e suas duas features serao removidas deste dispositivo. Personagens que futuramente a referenciarem poderao perder essas referencias.</p><div class="modal-actions"><button class="secondary-action" type="button" data-action="cancel-delete-compendium-ancestry">Cancelar</button><button class="danger-action" type="button" data-action="confirm-delete-compendium-ancestry">Excluir</button></div></section></div>`:"";}
export async function saveCompendiumAncestry(deps: AncestryFeatureDependencies):Promise<void>{const v=(s:string)=>document.querySelector<HTMLInputElement|HTMLTextAreaElement>(s)?.value.trim()??"",name=v("[data-ancestry-name]"),summary=v("[data-ancestry-summary]"),topName=v("[data-ancestry-top-name]"),topSummary=v("[data-ancestry-top-summary]"),bottomName=v("[data-ancestry-bottom-name]"),bottomSummary=v("[data-ancestry-bottom-summary]"),error=document.querySelector<HTMLElement>("[data-ancestry-error]"),existing=deps.state.editingCompendiumAncestryId?deps.catalog.ancestries.find(x=>x.id===deps.state.editingCompendiumAncestryId):undefined,duplicate=deps.catalog.ancestries.some(a=>a.name.toLocaleLowerCase("pt-BR")===name.toLocaleLowerCase("pt-BR")&&a.id!==existing?.id);if(!name||!summary||!topName||!topSummary||!bottomName||!bottomSummary||duplicate){if(error){error.hidden=false;error.textContent=duplicate?"Ja existe uma ancestralidade com este nome.":"Preencha a ancestralidade e as duas features.";}return;}if(existing&&existing.packId!=="local")return;try{const image=(await readImage("[data-ancestry-image]"))??existing?.image,id=existing?.id??`ancestry.local.${crypto.randomUUID()}`,topId=(existing?feature(existing,"top",deps.catalog)?.id:undefined)??`feature.local.${crypto.randomUUID()}`,bottomId=(existing?feature(existing,"bottom",deps.catalog)?.id:undefined)??`feature.local.${crypto.randomUUID()}`;const ancestry:AncestryDefinition={id,type:"ancestry",packId:"local",name,summary,image,topFeatureId:topId,bottomFeatureId:bottomId},top:FeatureDefinition={id:topId,type:"feature",packId:"local",name:topName,summary:topSummary,sourceType:"ancestry",sourceId:id,tier:"top"},bottom:FeatureDefinition={id:bottomId,type:"feature",packId:"local",name:bottomName,summary:bottomSummary,sourceType:"ancestry",sourceId:id,tier:"bottom"};await Promise.all([deps.saveCustomDefinition(ancestry),deps.saveCustomDefinition(top),deps.saveCustomDefinition(bottom)]);await deps.refreshCatalog();deps.state.ancestryModalOpen=false;deps.state.editingCompendiumAncestryId=undefined;deps.render();}catch(e){if(error){error.hidden=false;error.textContent=e instanceof Error?e.message:"Nao foi possivel salvar a ancestralidade.";}}}
export async function removeCompendiumAncestry(deps: AncestryFeatureDependencies):Promise<void>{const a=deps.state.deletingCompendiumAncestryId?deps.catalog.ancestries.find(x=>x.id===deps.state.deletingCompendiumAncestryId):undefined;if(!a||a.packId!=="local")return;await Promise.all([a.id,a.topFeatureId,a.bottomFeatureId].map(id=>deps.deleteCustomDefinition(id)));await deps.refreshCatalog();deps.state.deletingCompendiumAncestryId=undefined;deps.render();}
function renderAncestryResult(a:AncestryDefinition,deps:AncestryFeatureDependencies):string{const top=feature(a,"top",deps.catalog),bottom=feature(a,"bottom",deps.catalog),local=a.packId==="local",source=local?"Local":deps.getPackDisplayName(a.packId);return `<article class="compendium-class-result compendium-ancestry-result"><div class="compendium-class-result-open compendium-ancestry-result-open">${a.image?`<span class="compendium-class-image" style="background-image: url('${deps.escapeHtml(a.image)}')"></span>`:'<span class="compendium-class-image class-image-placeholder">ANCESTRALIDADE</span>'}<span class="compendium-class-body"><span>${deps.escapeHtml(source)}</span><h2>${deps.escapeHtml(a.name)}</h2><p>${deps.escapeHtml(a.summary)}</p><span class="ancestry-feature-pair"><span><b>Top Feature</b><strong>${deps.escapeHtml(top?.name??"Feature indisponivel")}</strong><small>${deps.escapeHtml(top?.summary??"")}</small></span><span><b>Bottom Feature</b><strong>${deps.escapeHtml(bottom?.name??"Feature indisponivel")}</strong><small>${deps.escapeHtml(bottom?.summary??"")}</small></span></span></span></div><div class="compendium-card-result-actions">${local?`<button type="button" data-action="edit-compendium-ancestry" data-ancestry-id="${deps.escapeHtml(a.id)}">Editar</button><button type="button" data-action="delete-compendium-ancestry" data-ancestry-id="${deps.escapeHtml(a.id)}">Excluir</button>`:'<span class="readonly-label">Conteúdo não editável</span>'}</div></article>`;}
function feature(a:AncestryDefinition,position:"top"|"bottom",c:Catalog){return c.features.find(f=>f.id===(position==="top"?a.topFeatureId:a.bottomFeatureId));}function featureFields(label:string,f:FeatureDefinition|undefined,key:"top"|"bottom",deps:AncestryFeatureDependencies){return `<div class="ancestry-feature-form-grid"><fieldset class="class-domain-field"><legend>${label} Feature</legend><label class="form-field"><span>Nome</span><input data-ancestry-${key}-name required value="${deps.escapeHtml(f?.name??"")}"/></label><label class="form-field"><span>Descricao</span><textarea data-ancestry-${key}-summary required>${deps.escapeHtml(f?.summary??"")}</textarea></label></fieldset></div>`;}function readImage(q:string):Promise<string|undefined>{const f=document.querySelector<HTMLInputElement>(q)?.files?.[0];if(!f)return Promise.resolve(undefined);if(f.size>1_500_000)return Promise.reject(new Error("A imagem deve ter no maximo 1,5 MB."));return new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(typeof r.result==="string"?r.result:undefined);r.onerror=()=>reject(new Error("Nao foi possivel ler a imagem."));r.readAsDataURL(f);});}
