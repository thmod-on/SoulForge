{
  "name": "soulforge",
  "version": "0.26.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "check:architecture": "node scripts/check-architecture.mjs",
    "check:release": "node scripts/check-release.mjs",
    "check": "pnpm run check:release && pnpm run check:architecture && tsc --noEmit && vitest run"
  },
  "dependencies": {
    "vite-plugin-pwa": "^0.21.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vitest": "^2.1.8"
  }
}
